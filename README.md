# Instrucciones para lanzar la aplicación
Es necesario tener instalado Docker para ejecutar la aplicación.

Después, basta con ejecutar el comando "docker-compose up --build" en la terminal, en el directorio del proyecto.